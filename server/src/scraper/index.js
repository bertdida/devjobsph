const config = require('./config');
const indeed = require('./indeed');
const glassdoor = require('./glassdoor');
const onlinejobs = require('./onlinejobs');

module.exports = {
  config,
  indeed,
  glassdoor,
  onlinejobs,
};
